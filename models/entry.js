// import uuid from 'uuidv4';
// import setup from "../database";

const {setup} = require('../database');
const {uuid} = require('uuidv4');

class entry {
    static async createEntry({id, date, food_health_rating, mood, food, user_id}) {

    }
}